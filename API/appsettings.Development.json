{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection" : "Data Source=dating.db"
  },
  "TokenKey": "This is the TokenKey for token from JWT This is the TokenKey for token from JWT This is the TokenKey for token from JWT" 
}
